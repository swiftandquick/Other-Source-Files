// Import the math.js file.  ./ means this directory, math is the js file name.  
const math = require('./math');

// Use functions and variables from math.js.  
console.log(math.add(1, 2));
console.log(math.PI);
console.log(math.square(9));