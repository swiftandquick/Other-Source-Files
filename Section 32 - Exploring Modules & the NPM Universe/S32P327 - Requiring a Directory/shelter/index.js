// Import the objects.  
const blue = require('./blue');
const sadie = require('./sadie');
const janet = require('./janet');

// Store the imported objects in the array.  
const allCats = [blue, sadie, janet];
// console.log(allCats);

// Export the array.  
module.exports = allCats;