<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Random</title>
</head>

<body>

    <h1>Your random number is: <%= num %>.</h1>

    <!-- Use JavaScript to print out a <h2> element based on whether the number is odd or even.  -->
    <% if (num % 2===0) { %>
        <h2>That's an even number!</h2>
        <% } else { %>
            <h2>That's an odd number!</h2>
            <% } %>

                <!-- Here, I use <%= %> to render the <h3> element instead.  -->
                <h3>That number is:
                    <%= num % 2===0 ? 'even' : 'odd' %>.
                </h3>

</body>

</html>